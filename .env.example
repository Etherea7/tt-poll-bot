# Template only. Export these values in your shell; the app does not load .env.
TELEGRAM_DESTINATIONS=test=-1001234567890

# Required only for an explicit --live run. Never commit a real token.
TELEGRAM_BOT_TOKEN=replace-with-botfather-token
